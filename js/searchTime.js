"use strict";

//let loader = require("./loader");

let currentTime = require("./time");

let searchedTime;

let eventTime;

//let events = character meetups + special events + etc

/* function eventsByTime(searchedTime) {
    for (var i = 0; )
} */

//module.exports = {  };